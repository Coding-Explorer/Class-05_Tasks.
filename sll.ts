//Example of Logical operators.

let c = 7
let d = 4
let logicalAnd = (c > 0) && (d > 0); //true statement.
let logicalOr = (c < 0) || (d > 0); //true statement.
let logicalNot = !(c > 0); //false statement.

console.log(logicalNot)